export default function Pokemon() {
  return (
    <div>
      <h3>This is the Pokemon index:</h3>

      {/* <ul>
        {posts.map((post) => (
          <li key={post}>
            <Link href={`/blog/${post}`}>{post}</Link>
          </li>
        ))}
      </ul> */}
    </div>
  );
}
